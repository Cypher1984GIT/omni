{
  "name": "omni",
  "version": "1.6.0",
  "description": "Omni - A multi-AI browser interface",
  "repository": "github:Cypher1984GIT/Omni",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "test": "node --check main.js && node --check preload-app.js && node --check preload-view.js && node --check renderer.js && node --check src/main/windowState.js && node --check src/main/security.js && node --check src/main/updater.js && node --check src/main/menu.js && node --check src/main/viewManager.js",
    "build": "electron-builder",
    "publish": "electron-builder --publish always"
  },
  "keywords": [],
  "author": "Cypher1984 <https://github.com/Cypher1984GIT>",
  "license": "ISC",
  "devDependencies": {
    "electron": "^39.2.7",
    "electron-builder": "^26.5.0"
  },
  "overrides": {
    "tar": "^7.5.6",
    "@electron/rebuild": {
      "tar": "^6.2.1"
    }
  },
  "build": {
    "appId": "com.omni.app",
    "productName": "Omni",
    "icon": "build/icon.png",
    "linux": {
      "target": [
        "AppImage",
        "deb"
      ],
      "category": "Utility;Network;WebBrowser;",
      "executableName": "omni",
      "maintainer": "Cypher1984 <https://github.com/Cypher1984GIT>",
      "icon": "build/icons"
    },
    "mac": {
      "target": [
        {
          "target": "dmg",
          "arch": [
            "universal"
          ]
        }
      ],
      "category": "public.app-category.utilities",
      "icon": "build/icon.png"
    },
    "win": {
      "target": "nsis",
      "icon": "icon.ico",
      "artifactName": "${productName}-Setup-${version}.${ext}"
    },
    "files": [
      "main.js",
      "index.html",
      "error.html",
      "icon.png",
      "icon.ico",
      "package.json",
      "preload.js",
      "preload-app.js",
      "preload-view.js",
      "renderer.js",
      "src/**/*",
      "styles.css",
      "tailwind-config.js"
    ],
    "publish": [
      {
        "provider": "github",
        "owner": "Cypher1984GIT",
        "repo": "Omni",
        "releaseType": "release"
      }
    ]
  },
  "dependencies": {
    "electron-log": "^5.4.3",
    "electron-updater": "^6.6.2"
  }
}
